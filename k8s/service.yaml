apiVersion: v1
kind: Service
metadata:
    name: edge-service
    namespace: polar
spec:
    selector:
        app: edge-service
    ports:
        -   protocol: TCP
            port: 80
            targetPort: 9000
    type: ClusterIP
  